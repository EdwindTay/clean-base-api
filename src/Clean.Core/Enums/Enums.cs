namespace Clean.Core
{
    public class Enums
    {
        public enum UserId
        {
            SystemUser = 1
        }

        public enum ActiveStatus
        {
            Deleted = 0,
            Active = 1,
            Inactive = 2,
        }
    }
}
